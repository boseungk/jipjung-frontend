# 작업 완료 시 체크리스트

## 코드 변경 후 확인사항

### 1. 기본 동작 확인
- [ ] 개발 서버가 정상 실행되는가? (`npm run dev`)
- [ ] 브라우저 콘솔에 에러가 없는가?
- [ ] 변경사항이 의도대로 동작하는가?

### 2. UI/UX 확인
- [ ] Day 모드에서 정상 표시되는가?
- [ ] Night 모드에서 정상 표시되는가?
- [ ] 4가지 컬러 테마 모두에서 정상 표시되는가?
  - 🏠 Warm Beige
  - 🌿 Olive Green
  - 🌫️ Cool Gray
  - ☁️ Sky Blue
- [ ] 반응형 레이아웃이 제대로 동작하는가? (데스크톱/태블릿/모바일)

### 3. 애니메이션/인터랙션
- [ ] GSAP 애니메이션이 부드럽게 동작하는가?
- [ ] 마우스 인터랙션이 정상 작동하는가? (조명 효과, 눈 효과 등)
- [ ] 페이지 전환/컴포넌트 마운트 시 애니메이션이 정상인가?

### 4. 상태 관리 (Pinia)
- [ ] 스토어 데이터가 올바르게 업데이트되는가?
- [ ] 컴포넌트 간 데이터 공유가 정상인가?
- [ ] LocalStorage 저장/로드가 제대로 동작하는가? (테마 설정 등)

### 5. 빌드 확인
- [ ] 프로덕션 빌드가 성공하는가? (`npm run build`)
- [ ] 빌드 결과물이 정상 동작하는가? (`npm run preview`)
- [ ] 빌드 크기가 적절한가? (과도하게 증가하지 않았는가?)

### 6. 코드 품질
- [ ] 불필요한 console.log가 제거되었는가?
- [ ] 사용하지 않는 import가 제거되었는가?
- [ ] 주석이 적절히 작성되었는가? (복잡한 로직에만)
- [ ] 변수/함수명이 의미를 명확히 전달하는가?

### 7. Git 커밋 전
- [ ] `git status`로 변경 파일 확인
- [ ] 의도하지 않은 파일이 포함되지 않았는가?
- [ ] `.gitignore` 규칙이 제대로 적용되었는가?
- [ ] 커밋 메시지가 명확한가? (feat:, fix:, refactor: 등)

## 새 컴포넌트 추가 시

### Vue 컴포넌트
- [ ] `<script setup>` 문법 사용
- [ ] 파일명이 PascalCase인가? (예: `MyComponent.vue`)
- [ ] 필요한 경우 props 정의 (`defineProps`)
- [ ] 필요한 경우 emits 정의 (`defineEmits`)
- [ ] 재사용 가능한 로직은 composable로 추출했는가?

### Pinia 스토어
- [ ] `use*Store` 네이밍 패턴 준수
- [ ] `defineStore`의 첫 번째 인자(store id)가 유니크한가?
- [ ] state, getters, actions 구조가 명확한가?

### Composable
- [ ] `use*` 네이밍 패턴 준수
- [ ] 재사용 가능하도록 순수 함수로 작성했는가?
- [ ] 필요한 값/함수만 return하는가?

## 스타일 변경 시

### Tailwind CSS
- [ ] 커스텀 클래스가 필요한지 재검토 (유틸리티로 해결 가능한가?)
- [ ] `tailwind.config.js`에 새 테마 추가 시 모든 모드에서 테스트

### 커스텀 CSS
- [ ] CSS 변수를 활용했는가? (테마 대응)
- [ ] 중복 스타일이 없는가?
- [ ] z-index 충돌이 없는가?

## 성능 체크
- [ ] 불필요한 리렌더링이 발생하지 않는가?
- [ ] watch/computed가 적절히 사용되었는가?
- [ ] 큰 데이터는 가상 스크롤 등으로 최적화했는가?
- [ ] 이미지 최적화가 되어 있는가? (SVG 최적화, 압축 등)

## 브라우저 호환성
- [ ] Chrome에서 정상 동작하는가?
- [ ] Firefox에서 정상 동작하는가?
- [ ] Safari에서 정상 동작하는가? (Mac 사용 가능 시)
- [ ] Edge에서 정상 동작하는가?

## 문서화 (필요시)
- [ ] README.md 업데이트가 필요한가?
- [ ] 중요한 변경사항은 docs/에 기록했는가?
- [ ] 복잡한 로직에 JSDoc 주석을 추가했는가?

## 현재 프로젝트에서 생략 가능한 항목
- 타입 체크 (TypeScript 미사용)
- 단위 테스트 (테스트 프레임워크 미설치)
- E2E 테스트 (테스트 프레임워크 미설치)
- 린팅 (ESLint 미설정)
- 포매팅 (Prettier 미설정)

이러한 항목들은 향후 프로젝트가 성장하면 추가 도입을 고려할 수 있습니다.
